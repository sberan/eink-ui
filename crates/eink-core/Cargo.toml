[package]
name = "eink-core"
version = "0.1.0"
edition = "2021"

[dependencies]
taffy = { version = "0.8", default-features = false, features = ["std", "flexbox", "content_size", "taffy_tree"] }
fontdue = "0.9"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
png = "0.17"
