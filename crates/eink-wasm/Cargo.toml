[package]
name = "eink-wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
eink-core = { path = "../eink-core" }

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
